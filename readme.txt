This program finds the number of appearances of a given word in a given word matrix.
Two letters are connected, if they are vertical or horizontal neighbors, not diagonal neighbors.
A matrix entry can not be used twice in a single word appearance, but can be used in multiple distinct word appearances

